# Run shared infra first:  docker compose -f ../../docker-compose.infra.yml up -d
name: vector-indexer-worker-deps

networks:
  free-ebay-local:
    external: true

services:
  # vector-indexer-worker:
  #   build: .
  #   environment:
  #     INDEXER_KAFKA_BOOTSTRAP_SERVER: "kafka:9092"
  #     INDEXER_KAFKA_GROUP_ID: "vector-indexer-worker"
  #     INDEXER_KAFKA_TOPICS: '["product.events"]'
  #     INDEXER_EMBEDDING_GRPC_URL: "embedding-service:50052"
  #     INDEXER_QDRANT_URL: "http://qdrant:6333"
  #     INDEXER_QDRANT_COLLECTION: "products"
  #   networks:
  #     - free-ebay-local
