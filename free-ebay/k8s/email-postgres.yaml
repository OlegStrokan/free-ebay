apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: email-postgres
  namespace: free-ebay
spec:
  serviceName: email-postgres
  replicas: 1
  selector:
    matchLabels:
      app: email-postgres
  template:
    metadata:
      labels:
        app: email-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
              name: postgres
          envFrom:
            - secretRef:
                name: email-postgres-secret
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "email_service"]
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "email_service"]
            initialDelaySeconds: 30
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 3Gi
---
apiVersion: v1
kind: Service
metadata:
  name: email-postgres
  namespace: free-ebay
spec:
  selector:
    app: email-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP