apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: free-ebay
  labels:
    app: product-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: free-ebay/product-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: grpc
          env:
            - name: ASPNETCORE_URLS
              value: http://+:8080
            - name: Kestrel__EndpointDefaults__Protocols
              value: Http2
          envFrom:
            - configMapRef:
                name: common-config
            - secretRef:
                name: product-service-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            grpc:
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            grpc:
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: free-ebay
spec:
  selector:
    app: product-service
  ports:
    - name: grpc
      port: 8080
      targetPort: 8080
  type: ClusterIP