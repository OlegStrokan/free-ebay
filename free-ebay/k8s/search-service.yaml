apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: free-ebay
  labels:
    app: search-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
    spec:
      containers:
        - name: search-service
          image: free-ebay/search-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: grpc
          env:
            - name: ASPNETCORE_URLS
              value: http://+:8080
            - name: Kestrel__EndpointDefaults__Protocols
              value: Http2
          envFrom:
            - configMapRef:
                name: common-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            grpc:
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            grpc:
              port: 8080
            initialDelaySeconds: 25
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: free-ebay
spec:
  selector:
    app: search-service
  ports:
    - name: grpc
      port: 8080
      targetPort: 8080
  type: ClusterIP