name: Vector Indexer Worker Pipeline

on:
  push:
    branches: [main]
    paths:
      - "AI/VectorIndexerWorker/**"
      - ".github/workflows/vector-indexer-worker-pipeline.yml"
  pull_request:
    paths:
      - "AI/VectorIndexerWorker/**"
      - ".github/workflows/vector-indexer-worker-pipeline.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.11"

jobs:
  build:
    name: Build (sync deps)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh
      - name: Cache uv
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: uv-${{ runner.os }}-${{ hashFiles('AI/VectorIndexerWorker/uv.lock') }}
          restore-keys: uv-${{ runner.os }}-
      - name: Sync dependencies
        working-directory: AI/VectorIndexerWorker
        run: uv sync --frozen

  test:
    name: ${{ matrix.tier }} Tests
    runs-on: ubuntu-latest
    needs: build
    strategy:
      fail-fast: false
      matrix:
        tier: [unit, integration, e2e]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh
      - name: Cache uv
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: uv-${{ runner.os }}-${{ hashFiles('AI/VectorIndexerWorker/uv.lock') }}
          restore-keys: uv-${{ runner.os }}-
      - name: Sync dependencies
        working-directory: AI/VectorIndexerWorker
        run: uv sync --frozen
      - name: Run ${{ matrix.tier }} tests
        working-directory: AI/VectorIndexerWorker
        run: uv run pytest tests/${{ matrix.tier }} --junitxml=../../test-results/vector-indexer-${{ matrix.tier }}.xml -v
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: vector-indexer-${{ matrix.tier }}-results
          path: test-results/vector-indexer-${{ matrix.tier }}.xml
      - uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Vector Indexer ${{ matrix.tier }}
          path: test-results/vector-indexer-${{ matrix.tier }}.xml
          reporter: java-junit
