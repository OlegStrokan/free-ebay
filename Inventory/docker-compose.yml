# Run shared infra first:  docker compose -f ../docker-compose.infra.yml up -d
name: inventory-service-deps

networks:
  free-ebay-local:
    external: true

volumes:
  inventory_postgres_data:

services:

  postgres:
    image: postgres:16-alpine
    container_name: inventory-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: inventory_service
    ports:
      - "5434:5432"
    volumes:
      - inventory_postgres_data:/var/lib/postgresql/data
    networks:
      - free-ebay-local
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d inventory_service"]
      interval: 5s
      timeout: 5s
      retries: 10

  # inventory-service:
  #   build: .
  #   environment:
  #     ASPNETCORE_ENVIRONMENT: Development
  #     ConnectionStrings__Postgres: "Host=postgres;Port=5432;Database=inventory_service;Username=postgres;Password=postgres"
  #     Kafka__BootstrapServers: "kafka:9092"
  #     Kestrel__EndpointDefaults__Protocols: Http2
  #     ASPNETCORE_URLS: "http://+:8080"
  #     Otel__Endpoint: "http://jaeger:4317"
  #   ports:
  #     - "8082:8080"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - free-ebay-local
